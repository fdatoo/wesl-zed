[package]
name = "wesl-zed"
version = "0.1.1"
edition = "2024"
license = "MIT"
repository = "https://github.com/fdatoo/wesl-zed"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

[profile.release]
codegen-units = 1
lto = true
panic = "abort"
strip = true
